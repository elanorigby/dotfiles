- defaults:

        link:
                relink: true

- clean: ['~']

- link:
        ~/.tmux:
        ~/.tmux.conf:
        ~/.vim:
        ~/.vimrc:
        ~/.vimrc: ~/.config/nvim/init.vim
        ~/.gitconfig:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
