- defaults:

        link:
                relink: true

- clean: ['~']

- link:
        ~/.bashrc:
        ~/.bash_profile:
        ~/.tmux.conf:
        ~/.vim:
        ~/.vimrc:
        ~/.gitconfig:
        ~/.git-completion.bash:
        ~/.gitignore_global:
        ~/.config/nvim: nvim
        ~/.ideavimrc:
        ~/.bash_profile:
        ~/.bashrc:



- shell:
  - [git submodule update --init --recursive, Installing submodules]
