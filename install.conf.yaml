- defaults:

        link:
                relink: true

- clean: ['~']

- link:
        ~/.tmux.conf:
        ~/.vim:
        ~/.vimrc:
        ~/.gitconfig:
        ~/.config/nvim: nvim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
