- defaults:

        link:
                relink: true

- clean: ['~']

- link:
        ~/.tmux.conf:
        ~/.vim:
        ~/.vimrc:
        ~/.gitconfig:
        ~/.git-completion.bash:
        ~/.config/nvim: nvim
        ~/.ideavimrc:


- shell:
  - [git submodule update --init --recursive, Installing submodules]
